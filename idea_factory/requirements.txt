# idea_factory - Business Idea Factory MVP
# Requires Python 3.10+ for strands-agents
Django>=4.2,<6
psycopg[binary]>=3.1
python-dotenv>=1.0
strands-agents[ollama,openai]>=1.0.0
pydantic>=2.0
requests>=2.28
pypdf>=4.0

# Google Calendar sync (optional)
google-api-python-client>=2.100
google-auth>=2.23
google-auth-oauthlib>=1.2

# Optional: for web search in InternetExplorerAgent (ddgs is the maintained fork)
ddgs>=9.0

# Production WSGI server (Docker)
gunicorn>=22.0

# Testing
pytest>=7.0
pytest-django>=4.5
